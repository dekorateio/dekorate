#
# When container autoscaling is configured, check that the provided `containerConcurrency` is respected.
#
dekorate.options.input-path=kubernetes
dekorate.knative.revision-autoscaling.target-utilization-percentage=50
